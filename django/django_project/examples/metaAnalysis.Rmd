suppressMessages({library(dplyr)
library(meta)
library(readxl)
library(robumeta)
library(grid)
library(here)
library(forestplot)
library(irr)})

a <- 100

#here is a little example code that proves synchronicity 
#write("R is sleeping", stderr())
#Sys.sleep(10)
#write("ZZZ", stderr())
#Sys.sleep(10)

#import data and place in meta_study and meta_SNP
meta_study <- read_excel(here("django/django_project/DisorderData.xlsx"), 
    sheet = "studyData", na = ".")
	
meta_SNP <- read_excel(here("django/django_project/DisorderData.xlsx"), 
    sheet = "SNP_entryData", na = ".")
	
#flag for console to let you know that R is currently running
write("R is currently performing meta-analysis", stderr())




